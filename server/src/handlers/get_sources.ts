
import { type GetSourcesInput, type Source } from '../schema';

export const getSources = async (input: GetSourcesInput): Promise<Source[]> => {
  // This is a placeholder declaration! Real code should be implemented here.
  // The goal of this handler is fetching all sources attached to a specific document.
  // Should verify that the user has access to the document before returning sources.
  return [];
};
